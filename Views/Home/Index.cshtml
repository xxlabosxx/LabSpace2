@model LabSpace2.ViewModel.HomeViewModel

@{
    ViewData["Title"] = "Cardapio";
}

<partial name="_Carousel">
<h1 class="text-light text-center mt-3 kumar">Itens em produção</h1>
</partial>
<div class="row">
@foreach(var item in Model.ItensEmDestaque){
<partial name="_Card" for="@item"></partial>
}
</div>
